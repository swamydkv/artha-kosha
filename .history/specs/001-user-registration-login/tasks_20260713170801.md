---
description: "Task list template for feature implementation"
---

# Tasks: User Registration and Login

**Input**: Design documents from `/specs/001-user-registration-login/`

**Prerequisites**: plan.md (required), spec.md (required for user stories), research.md, data-model.md, contracts/

**Tests**: Tests are mandatory for this feature because the constitution requires TDD and acceptance coverage for every feature slice.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2)
- Include exact file paths in descriptions

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization and basic structure

- [ ] T001 Create the repository structure per the implementation plan in `apps/finance-api/`, `apps/web/`, `api/`, `infra/`, and `docs/`
- [ ] T002 [P] Initialize the Go module and shared backend dependencies in `apps/finance-api/go.mod`
- [ ] T003 [P] Initialize the Next.js TypeScript app in `apps/web/package.json`
- [ ] T004 [P] Configure Go and frontend linting/formatting in `apps/finance-api/.golangci.yml` and `apps/web/.eslintrc.json`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core infrastructure that MUST be complete before ANY user story can be implemented

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [ ] T005 Create Docker Desktop orchestration for Postgres, API, and web in `infra/docker-compose.yml`
- [ ] T006 [P] Finalize the canonical authentication OpenAPI contract in `api/auth.yaml`
- [ ] T007 [P] Define PostgreSQL migration files in `apps/finance-api/migrations/001_create_users_table.sql` and `apps/finance-api/migrations/002_create_sessions_table.sql`
- [ ] T008 [P] Create `sqlc` configuration and query definitions under `apps/finance-api/sql/`
- [ ] T009 Create shared auth configuration and environment wiring in `apps/finance-api/internal/config/config.go`
- [ ] T010 Create password hashing and session validation primitives in `apps/finance-api/internal/auth/security.go`
- [ ] T011 Create the base HTTP routing and middleware skeleton in `apps/finance-api/internal/http/router.go`
- [ ] T011A [P] Create the dedicated authentication abstraction module in `apps/finance-api/internal/auth/provider.go`

**Checkpoint**: Foundation ready - user story implementation can now begin in parallel

---

## Phase 3: User Story 1 - Register New User (Priority: P1) 🎯 MVP

**Goal**: Accept, validate, and persist a new account within a single ACID transaction, then redirect the user to the login screen.

**Independent Test**: A new registration request with valid unique values finishes successfully and the UI routes to the login flow without leaking private state.

### Tests for User Story 1 ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [ ] T012 [P] [US1] Add contract test for `POST /register` in `apps/finance-api/internal/auth/contract/register_contract_test.go`
- [ ] T013 [P] [US1] Add integration test for successful registration in `apps/finance-api/internal/auth/integration/register_success_test.go`
- [ ] T014 [P] [US1] Add regression test for duplicate username/email/mobile rejection in `apps/finance-api/internal/auth/integration/register_conflict_test.go`
- [ ] T014A [P] [US1] Add unit tests for registration validation rules in `apps/finance-api/internal/auth/validation/register_validation_test.go`
- [ ] T014B [P] [US1] Measure registration latency and capture benchmark results in `apps/finance-api/internal/auth/integration/register_benchmark_test.go`

### Implementation for User Story 1

- [ ] T015 [P] [US1] Create user domain types and repository contract in `apps/finance-api/internal/users/model.go` and `apps/finance-api/internal/users/repository.go`
- [ ] T016 [US1] Implement registration service logic in `apps/finance-api/internal/auth/register_service.go`
- [ ] T017 [US1] Implement `POST /register` handler in `apps/finance-api/internal/http/register_handler.go`
- [ ] T018 [P] [US1] Create the registration UI in `apps/web/app/register/page.tsx` with inline validation and the create-account action
- [ ] T019 [US1] Add the post-registration redirect flow to the login page in `apps/web/app/register/success/page.tsx` or the client-side registration flow

**Checkpoint**: At this point, User Story 1 should be fully functional and testable independently

---

## Phase 4: User Story 2 - Authenticate User (Priority: P1)

**Goal**: Authenticate the user, establish the session, and provide the minimal signed-in home view with a logout path.

**Independent Test**: A valid username/password pair signs the user in and the logout action returns the app to a signed-out session-ended state.

### Tests for User Story 2 ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [ ] T020 [P] [US2] Add contract test for `POST /login` in `apps/finance-api/internal/auth/contract/login_contract_test.go`
- [ ] T021 [P] [US2] Add integration test for the login/logout flow in `apps/finance-api/internal/auth/integration/auth_flow_test.go`
- [ ] T022 [P] [US2] Add regression test for generic authentication errors in `apps/finance-api/internal/auth/integration/auth_error_test.go`
- [ ] T022A [P] [US2] Add unit tests for login credential and session validation in `apps/finance-api/internal/auth/validation/login_validation_test.go`
- [ ] T022B [P] [US2] Measure login latency and capture benchmark results in `apps/finance-api/internal/auth/integration/login_benchmark_test.go`

### Implementation for User Story 2

- [ ] T023 [P] [US2] Implement credential validation and session creation in `apps/finance-api/internal/auth/login_service.go`
- [ ] T024 [US2] Implement `POST /login` and `POST /logout` handlers in `apps/finance-api/internal/http/auth_handlers.go`
- [ ] T025 [P] [US2] Create the login page in `apps/web/app/login/page.tsx`
- [ ] T026 [US2] Create the authenticated home view and logout action in `apps/web/app/home/page.tsx`
- [ ] T027 [US2] Add protected-route/session guard logic in `apps/web/lib/auth/session.ts` or `apps/web/middleware.ts`

**Checkpoint**: At this point, User Stories 1 and 2 should both work independently

---

## Phase 5: Polish & Cross-Cutting Concerns

**Purpose**: Improvements that affect multiple user stories

- [ ] T028 [P] Update `docs/` with the finalized architecture, Mermaid diagrams, exact user flow, and call flow for the authentication MVP
- [ ] T029 Run `quickstart.md` validation against `infra/docker-compose.yml`
- [ ] T030 [P] Add final regression coverage for auth edge cases in `apps/finance-api/internal/auth/integration/auth_edge_cases_test.go`
- [ ] T031 Verify performance thresholds and save benchmark evidence for registration and login in `docs/`
- [ ] T032 Add or update coverage-exception documentation where any authentication file falls below 100% coverage after justified review
- [ ] T033 Code cleanup and refactoring across shared auth modules

> **Final Docs Rule**: Documentation updates in `docs/` MUST happen at the end of the feature lifecycle, after the implementation and verification steps are stable.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies - can start immediately
- **Foundational (Phase 2)**: Depends on Setup completion - BLOCKS all user stories
- **User Stories (Phase 3+)**: All depend on Foundational phase completion
  - User stories can then proceed in parallel (if staffed)
  - Or sequentially in priority order (P1 → P2)
- **Polish (Final Phase)**: Depends on all desired user stories being complete

### User Story Dependencies

- **User Story 1 (P1)**: Can start after Foundational (Phase 2) - No dependencies on other stories
- **User Story 2 (P1)**: Can start after Foundational (Phase 2) - May integrate with US1 but should be independently testable

### Within Each User Story

- Tests MUST be written and FAIL before implementation
- Domain model before service implementation
- Services before API handlers
- Frontend screens after backend paths are stable
- Story complete before moving to the next priority

### Parallel Opportunities

- All Setup tasks marked [P] can run in parallel
- All Foundational tasks marked [P] can run in parallel (within Phase 2)
- Once Foundational is complete, US1 and US2 can begin in parallel if the team has capacity
- All tests for each user story marked [P] can run in parallel
- Different stories can be worked on in parallel by different team members

---

## Parallel Example: User Story 1

```bash
# Launch the registration contract and integration tests together:
Task: "Add contract test for POST /register in apps/finance-api/internal/auth/contract/register_contract_test.go"
Task: "Add integration test for successful registration in apps/finance-api/internal/auth/integration/register_success_test.go"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational (CRITICAL - blocks all stories)
3. Complete Phase 3: User Story 1
4. **STOP and VALIDATE**: Test the registration flow independently
5. Deploy/demo if ready

### Incremental Delivery

1. Complete Setup + Foundational → Foundation ready
2. Add User Story 1 → Test independently → Deploy/Demo (MVP)
3. Add User Story 2 → Test independently → Deploy/Demo
4. Finalize documentation and cross-cutting concerns

### Parallel Team Strategy

With multiple developers:

1. Team completes Setup + Foundational together
2. Once Foundational is done:
   - Developer A: User Story 1 registration flow
   - Developer B: User Story 2 login/logout flow
3. Stories complete and integrate independently

---

## Notes

- [P] tasks = different files, no dependencies
- [Story] label maps each task to the relevant story for traceability
- Each user story should be independently completable and testable
- Verify tests fail before implementing
- Stop at any checkpoint to validate the story independently
- Avoid vague tasks, same-file conflicts, and cross-story dependencies that break independence
